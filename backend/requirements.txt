# fastapi is the modern, fast web framework we use to build our API endpoints
fastapi==0.110.0

# uvicorn is the lightning-fast ASGI server that actually runs our FastAPI application
uvicorn==0.28.0

# groq is the official software development kit (SDK) to talk to Groq's high-speed AI models
groq>=0.9.0

# pydantic-settings extends Pydantic to read environment variables from our .env file securely
pydantic-settings==2.2.1

# python-dotenv parses key-value pairs from a .env file and sets them as environment variables
python-dotenv==1.0.1
